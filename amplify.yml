version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site build"
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - style.css
      - app.js
      - favicon.png
      - app-ads.txt
      - "assets/**/*"
      - "pages/**/*"
  cache:
    paths: []
